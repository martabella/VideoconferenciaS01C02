# 🚀 Demo de Programación Java - Febrero 2026

Este repositorio contiene los ejercicios y retos desarrollados durante las sesiones de videoconferencia de los días **11 y 18 de febrero**. El objetivo de estas clases ha sido asentar las bases de la **programación estructurada** en Java, enfocándonos en la interacción con el usuario, la validación de datos y el control de flujo.

---

## 📋 Contenido del Proyecto

A continuación, se detallan los archivos incluidos y las funcionalidades que implementa cada uno:

### 1. Estructuras de Control y Lógica de Acceso
* **`Main.java`**: Simulación de un sistema de control de acceso que verifica la mayoría de edad mediante una constante y gestiona el pago de una entrada con cálculo de cambio.
* **`Ejemplo1.java`**: Ejemplo comparativo que muestra cómo transformar una estructura de múltiples `if-else` en una sentencia `switch` para clasificar niveles de aprendizaje.

### 2. Cálculos y Validación de Tipos
* **`Ejercicio7.java`**: Calculadora de PVP que utiliza constantes para el IVA (21%) y descuentos (10%). Incluye validación de entrada con `hasNextFloat()` para asegurar que el usuario introduzca números válidos.

### 3. Retos de Programación (Gamificación)
* **`Challenge1.java` (Número Mágico)**: Juego interactivo donde el usuario define los límites (mínimo y máximo) y debe adivinar un número aleatorio generado por la clase `Random`.
* **`Challenge2.java` (Piedra, Papel o Tijera)**: Implementación del juego clásico contra la CPU utilizando una estructura `switch` para evaluar las condiciones de victoria, empate o derrota.

---

## 🧠 Conceptos Técnicos Trabajados

Durante estas sesiones, hemos explorado los siguientes pilares fundamentales:

* **Entrada de datos robusta**: Uso de la clase `Scanner` combinada con métodos de comprobación como `hasNextInt()` para evitar errores en tiempo de ejecución.
* **Gestión del Buffer**: Uso de `input.nextLine()` tras lecturas numéricas para limpiar el salto de línea y evitar errores en la captura de datos posteriores.
* **Estructuras de Decisión**: Implementación de condicionales if-else anidados y sentencias `switch` para manejar múltiples escenarios lógicos.
* **Uso de Constantes**: Empleo de la palabra clave `final` para definir valores fijos como el IVA o la edad mínima, mejorando la mantenibilidad del código.

---

## 🛠️ Requisitos del Entorno

Para ejecutar y compilar estos ejemplos de forma óptima, se requiere:
* **JDK 24**: El proyecto está alineado con las últimas versiones de Java para garantizar compatibilidad.
* **Editor de Código**: IntelliJ IDEA, VS Code con Java Extension Pack o Eclipse.

---
